<!--components/movie-card/index.wxml-->
<view class="movie-card">
  <image class="movie-cover"
         src="{{movie.image}}"></image>
  <text class="movie-name">{{movie.name}}</text>
  <stars rating="{{movie.rating}}"  stars="{{movie.stars}}"></stars>
</view>
